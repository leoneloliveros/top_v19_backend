version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node
    working_directory: ~/project
    steps:
      - checkout
      - run:
          name: Instalando dependencias para llevar a produccion
          command: npm install
      - run:
          name: Corriendo el lint para probar el proyecto
          command: npm run lint
  test1:
    docker:
      - image: circleci/node
    working_directory: ~/project
    steps:
      - checkout
      - run:
          name: Instalando dependencias para llevar a produccion
          command: npm install
      - run:
          name: Probando conexion a correos
          command: npm run sendEmail

workflows:
  heroku_deploy:
    jobs:
      - build
      - test1



